# goit-markup-hw-05

/ ====================== Home Work#2 ====================== /

1. Поставить логотипу атрибут arial-label="Логотип веб студии для повышения доступности"
2. Кнопка = Type button - если это просто кнопка (открывается модальное окно), type submit - отправляет запрос на
   сервер.
3. Можно создавать несколько папок в папке image для удобства.
4. Перейменовать картинки команды по их именам.
5. Тег hidden скрывает элемент из потока контента, но и от читалок.
6. <address>
   <ul>
        <li><a href="ссылка на гугл карты" class="link">адрес</a></li>

/ ====================== Home Work#3 ====================== /

Сестринский селектор + Селектор соседа ~ наследуются - text-... / font-... / color-... / line-height (наследуются только
текстовые свойства)

если у элемента точечно заданы стили он их не наследует

# opacity - задает прозрасность НО будет прозрачным ВСЕ, в том числе и текст

h1, h2... { margin: 0; }

ul,ol { margin:0; padding:0 }

(списки) .list { list-style: none; }

(ссылки) .link { text-decoration: none; }

(картинки) .img { display: block; }

Формула width: calc((100%-90px) / 3);

/ ====================== Home Work#4 ====================== /

background-origin: padding-top - позиционирует изображение в блочной модели

/ ====================== Home Work#5 ====================== /
